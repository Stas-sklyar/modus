import { ApplyRedirectsToMentionsDirective } from './apply-redirects-to-mentions.directive';

describe('TransformMentionsToLinksDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyRedirectsToMentionsDirective();
    expect(directive).toBeTruthy();
  });
});
