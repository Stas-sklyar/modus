import { DocumentsTotalAmountPipe } from './documents-total-amount.pipe';

describe('DocumentsTotalAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentsTotalAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
