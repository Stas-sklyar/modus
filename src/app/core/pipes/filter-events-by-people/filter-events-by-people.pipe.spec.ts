import { FilterEventsByPeoplePipe } from './filter-events-by-people.pipe';

describe('FilterEventsByPeoplePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEventsByPeoplePipe();
    expect(pipe).toBeTruthy();
  });
});
