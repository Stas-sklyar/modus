import { GroupEventsByYearPipe } from './group-events-by-year.pipe';

describe('GroupEventsByYearPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupEventsByYearPipe();
    expect(pipe).toBeTruthy();
  });
});
