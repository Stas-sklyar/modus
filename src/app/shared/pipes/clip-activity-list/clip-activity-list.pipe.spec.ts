import { ClipActivityListPipe } from './clip-activity-list.pipe';

describe('ClipListLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new ClipActivityListPipe();
    expect(pipe).toBeTruthy();
  });
});
